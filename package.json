{
  "name": "codex-cli-practice-project",
  "version": "1.0.0",
  "description": "Codex CLI 사용법을 연습하기 위한 프로젝트입니다.",
  "main": "server.js",
  "scripts": {
    "build": "npm install --include=dev --prefix frontend && npm run build --prefix frontend",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.18.3",
    "mongodb": "^7.2.0",
    "multer": "^2.1.1",
    "socket.io": "^4.8.3"
  }
}
